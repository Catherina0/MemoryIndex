# ========== OCR 引擎（二选一） ==========
# 选项 1: PaddleOCR（跨平台，需下载模型）
# paddlepaddle>=3.0.0
# paddleocr>=2.7.0
# opencv-python
# numpy

# 选项 2: Apple Vision OCR（macOS 原生，无需依赖）
# 无需额外安装，仅需 macOS 10.15+ 和系统自带 Swift
# 💡 推荐 macOS 用户使用 Vision OCR（速度快，零配置）

# ========== 基础依赖 ==========
numpy 
groq
google-genai>=0.2.0      # Gemini API (用于处理超长文本)
python-dotenv
yt-dlp
tqdm  # 进度条显示

# 数据库与搜索（新增）
tabulate>=0.9.0          # 命令行表格输出

# 全文搜索（中文支持）
Whoosh>=2.7.4            # 纯Python全文搜索引擎
jieba>=0.42.1            # 中文分词

# 小红书下载支持（可选）
httpx>=0.27.0            # XHS-Downloader 依赖
aiosqlite>=0.20.0        # XHS-Downloader 依赖
aiofiles>=24.0.0         # XHS-Downloader 依赖

# 网页归档支持（新增）
crawl4ai>=0.2.0          # 智能爬虫框架（推荐）
playwright>=1.40.0       # 浏览器自动化
beautifulsoup4>=4.12.0   # HTML解析
html2text>=2024.0.0      # HTML转Markdown
browser-cookie3>=0.19.0  # 浏览器Cookie提取（可选）
tabulate
whoosh
python-dotenv

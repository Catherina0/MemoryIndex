╔═══════════════════════════════════════════════════════════════╗
║          📹 Video Report Pipeline - 快速参考卡片               ║
║🆕 DrissionPage浏览器 + 图片下载 + 网页归档 + 数据库 + OCR    ║
╚═══════════════════════════════════════════════════════════════╝

📍 项目位置
───────────────────────────────────────────────────────────────
/Users/catherina/Documents/GitHub/knowledge/

✨ 新特性（2026-01-24）🆕
───────────────────────────────────────────────────────────────
• 🧠 双 LLM 灵活切换 - 本地 OSS 与 Gemini 自动或强制切换
• 🎤 双 ASR 模型支持 - Whisper Large V3 (默认高质量) 与 Turbo (高速低成本)
• 📏 智能 Token 估算 - 中文 1:1 / 英文 2:1 精准触发长文本模式
• 📝 报告升级 - 显示实际使用的模型名称 (摘要+详细)

✨ 新特性（2026-01-08）
───────────────────────────────────────────────────────────────
• 📊 增强的数据库状态查看 - 5大类详细统计（基础/来源/状态/类型/健康）
• 📈 新增详细统计命令 - db-stats（归档/视频/标签专项统计）
• 🗑️  数据库删除功能 - db-delete（安全删除记录+搜索索引）
• 🌐 网页归档专项统计 - 按平台分类、OCR使用率、最近归档
• 🎥 视频文件专项统计 - 平台统计、总时长、平均文件大小
• 🏷️  标签使用统计 - Top 20最常用标签、使用分布
• ✅ 处理成功率监控 - 自动计算成功率和失败记录
• 📅 活跃度统计 - 最近7天处理记录数

✨ 新特性（2026-01-07）
───────────────────────────────────────────────────────────────
• 🆕 DrissionPage 归档器 - 真实浏览器、完整JS、自动登录态
• 🆕 图片自动下载 - HTTP + base64 → 本地JPG，自动更新引用
• 🌐 双引擎归档 - Crawl4AI（快）+ DrissionPage（强）
• 🔐 一次登录长期使用 - 浏览器数据自动保存，支持多平台
• 🔗 支持多平台 - 知乎、小红书、B站、Reddit、推特等
• 🍪 Cookie统一管理 - 一次配置，视频下载+网页归档都能用
• 🤖 自动平台识别 - 检测URL自动选择最佳策略
• 💾 处理视频后自动保存到数据库（视频信息+转写+OCR+报告+标签）
• 🔍 AI自动提取标签和主题章节
• 📊 支持全文搜索和标签搜索
• 📹 列出所有处理过的视频（名称+标签+摘要）
• 🎯 首次运行自动创建虚拟环境和安装依赖
• 🤖 支持选择不同性能级别的 OCR 模型
• 🆕 统一视频下载层 - 支持 YouTube、Bilibili、小红书等
• 🆕 智能降级策略 - yt-dlp → BBDown → XHS-Downloader
• 🆕 从URL直接处理视频 - 下载+抽帧+OCR+ASR+总结+入库一键完成

🚀 核心命令（最常用）
───────────────────────────────────────────────────────────────
┌─ 本地视频：音频模式（快速）──────────────────────────────────┐
│ make run VIDEO=视频路径                                      │
│ 流程：音频提取 → Groq转写 → AI总结 → 💾 入库                │
│ 适合：播客、会议、讲座                                       │
│ 速度：⚡⚡⚡ 快                                              │
│ 入库：自动提取标签、主题、时间线并保存到数据库               │
└────────────────────────────────────────────────────────────┘

┌─ 本地视频：OCR模式（完整）───────────────────────────────────┐
│ make ocr VIDEO=视频路径                                      │
│ 流程：1️⃣ OCR识别 → 2️⃣ 音频转写 → 3️⃣ AI总结 → 💾 入库      │
│ 特点：先处理OCR确认无误，再处理音频                          │
│ 适合：教学视频、演示PPT、代码演示                            │
│ 速度：🐢 慢（带进度条实时显示）                              │
│ 入库：保存转写+OCR+报告+标签+主题+时间线                    │
└────────────────────────────────────────────────────────────┘

┌─ 🆕 从URL下载并处理：仅音频 ─────────────────────────────────┐
│ make download-run URL="https://www.youtube.com/watch?v=xxx" │
│ make download-run URL="朋友分享的视频：链接在这..."           │
│ 流程：下载 → 音频提取 → Groq转写 → AI总结 → 💾 入库          │
│ 支持：YouTube, Bilibili, 小红书, 抖音, Twitter等            │
│ 特点：✨自动从文本中提取URL，自动下载到 videos/             │
│ 速度：⚡⚡ 中等（含下载时间）                                │
└────────────────────────────────────────────────────────────┘

┌─ 🆕 从URL下载并处理：完整OCR ────────────────────────────────┐
│ make download-ocr URL="https://www.bilibili.com/video/BVxxx"│
│ make download-ocr URL="看这个视频很有趣：链接..."            │
│ 流程：下载 → 抽帧 → OCR → 音频 → AI总结 → 💾 入库            │
│ 特点：✨自动从分享文本中提取URL，所有平台统一处理             │
│ 速度：🐢 慢（含下载+OCR）                                    │
└────────────────────────────────────────────────────────────┘

┌─ 本地视频：OCR模式 + 模型选择 ──────────────────────────────┐
│ # 高精度模式（推荐重要内容）                                  │
│ make ocr VIDEO=xxx DET_MODEL=server REC_MODEL=server        │
│                                                              │
│ # 快速模式（推荐日常使用，默认）                              │
│ make ocr VIDEO=xxx DET_MODEL=mobile REC_MODEL=mobile        │
│                                                              │
│ # 平衡模式（性价比最高）                                      │
│ make ocr VIDEO=xxx DET_MODEL=mobile REC_MODEL=server        │
│                                                              │
│ 💡 OCR 已优化参数，可识别：                                  │
│    ✓ 中文字幕  ✓ 英文文本  ✓ 数字  ✓ 混合文本               │
│    • 检测阈值: 0.2 (更敏感)                                  │
│    • 置信度过滤: 0.25 (保留更多结果)                         │
│    • 文本框扩展: 1.8x (捕获边缘文本)                         │
└────────────────────────────────────────────────────────────┘

📝 示例
───────────────────────────────────────────────────────────────
本地文件处理：
  make run VIDEO=~/Downloads/meeting.mp4
  make ocr VIDEO=~/Desktop/tutorial.mp4

模型参数选择（新功能）：
  make run VIDEO=x.mp4 LLM=gemini       # 强制使用 Gemini
  make run VIDEO=x.mp4 ASR=turbo        # 使用 Whisper Turbo
  make ocr VIDEO=x.mp4 LLM=oss ASR=v3   # 组合使用

URL下载处理 - 直接链接：
  make download-run URL="https://www.youtube.com/watch?v=dQw4w9WgXcQ"
  make download-ocr URL="https://www.bilibili.com/video/BV1xx411c7mD"

✨ URL下载处理 - 自动提取（分享文本）：
  make download-run URL="朋友推荐的视频：https://youtu.be/xxx 非常有趣"
  make download-ocr URL="B站分享 - https://b23.tv/abc123 - 必看"

仅下载（不处理）：
  make download URL="https://www.youtube.com/watch?v=xxxxx"

💡 URL提取功能 - 智能识别
───────────────────────────────────────────────────────────────
✨ 无需手动提取，直接粘贴分享内容：
  • 支持完整URL：https://www.bilibili.com/video/BVxxx
  • 支持短链接：https://b23.tv/abc123
  • 支持分享文本：分享一个视频给你：链接在这 https://...
  • 自动清理尾部标点：链接）、链接。等都能识别

系统自动识别 → 提取 → 下载处理，无需手动操作！

🔧 环境配置
───────────────────────────────────────────────────────────────
🆕 自动模式（推荐）：
  直接运行 make run 或 make ocr，首次会自动：
  • 创建虚拟环境 (.venv)
  • 安装所有依赖（包括 yt-dlp 用于下载）
  • 运行环境自检 (make selftest)
  • 创建配置文件模板

手动模式（可选）：
  make setup          # 手动初始化环境
  nano .env           # 填入 GROQ_API_KEY
  make selftest       # 完整系统自检（推荐）
  make check          # 快速配置验证

🔍 系统自检
───────────────────────────────────────────────────────────────
运行 make selftest 可进行全系统健康检查：

检查项目（12项）：
  1️⃣  核心模块导入 - 验证代码完整性
  2️⃣  依赖库检查 - 检查必需/可选依赖
  3️⃣  数据库状态 - 查看数据统计
  4️⃣  Whoosh搜索 - 中文搜索引擎
  5️⃣  搜索功能 - FTS+Whoosh测试
  6️⃣  下载器状态 - 视频下载功能
  7️⃣  FFmpeg检查 - 音视频处理工具
  8️⃣  API配置 - GROQ_API_KEY验证
  9️⃣  磁盘空间 - 存储空间监控
  🔟 网页归档功能 - 平台检测+适配器
  1️⃣1️⃣ Cookie管理 - 统一Cookie配置
  1️⃣2️⃣ 归档集成 - Markdown转换+文档

输出示例：
  ✅ 所有检查通过！系统运行正常
  
  🎯 功能状态:
     ✅ 视频处理系统
     ✅ 视频下载系统
     ✅ 数据库与搜索
     ✅ 网页归档系统
     ✅ Cookie统一管理
  
  💡 快速开始:
     • 处理视频: make run VIDEO=video.mp4 [参数]
     • 下载视频: make download-run URL=视频链接
     • 归档网页: make archive URL=网页链接
     • 搜索内容: make search Q='关键词'

  💡 高级视频处理参数:
     • LLM选择: LLM=gemini (强制使用) 或 LLM=oss (强制本地模型)
     • ASR选择: ASR=turbo (更快) 或 ASR=v3 (更准, 默认)
     
     示例:
       make run VIDEO=x.mp4 LLM=gemini       # 强制使用 Gemini
       make run VIDEO=x.mp4 ASR=turbo        # 使用 Turbo 加速转写
       make ocr VIDEO=x.mp4 LLM=oss ASR=v3   # 强制本地LLM + 精准ASR

常见问题修复：
  • 依赖缺失：make install
  • 浏览器缺失：playwright install chromium
  • Cookie未配置：make config-xhs-cookie
  • 数据库问题：make db-init

🌐 支持的视频平台
───────────────────────────────────────────────────────────────
✅ 完全支持：
  • YouTube (youtube.com, youtu.be)
  • Bilibili (bilibili.com, b23.tv)

⚠️ 需配置：
  • 小红书 (xiaohongshu.com) - 需安装 XHS-Downloader
  • 抖音 (douyin.com) - 需测试
  • Twitter/X (twitter.com, x.com) - 需测试

🎨 下载文件命名规范
───────────────────────────────────────────────────────────────
格式：标题_平台_视频ID.mp4

示例：
  Never_Gonna_Give_You_Up_youtube_dQw4w9WgXcQ.mp4
  某个有趣的视频标题_bilibili_BV1xx411c7mD.mp4
  INTP：你不是迷茫而是在逃避真正的目标_bilibili_BV1ngCyBiEkc.mp4

特点：
  • 文件名以标题开头，易于识别内容
  • 包含平台标记，便于溯源
  • 包含视频ID，便于快速查找
  • 存储在 videos/ 目录

存储位置：videos/ 目录

🛠️ 维护命令
───────────────────────────────────────────────────────────────
make help           # 查看所有命令（含下载命令）
make test           # 运行环境测试
make check          # 检查配置状态
make clean          # 清理输出文件
make show-report    # 查看最新报告
make list-reports   # 列出所有报告

�️  数据库快捷命令🆕
───────────────────────────────────────────────────────────────
make db-status      # 📊 查看数据库状态（5大类统计）
make db-stats       # 📈 详细统计（归档/视频/标签）
make db-show ID=1   # 👁️  查看特定记录详情
make db-delete ID=1 # 🗑️  删除记录（需确认）
make search Q="关键词"  # 🔍 全文搜索

�🔍 OCR 调试工具
───────────────────────────────────────────────────────────────
python test_ocr_debug.py --check-api              # 检查OCR状态
python test_ocr_debug.py --image xxx.png          # 测试单张图片
python test_ocr_debug.py --folder frames/         # 批量测试
python test_ocr_debug.py --compare-thresholds     # 对比阈值
python test_ocr_debug.py --help                   # 查看帮助

📚 详细文档: docs/OCR_DEBUG_GUIDE.md

📂 输出位置
───────────────────────────────────────────────────────────────
output/
└── 视频名_时间戳/          # 每次处理独立文件夹
    ├── 视频名.wav          # 提取的音频
    ├── frames/            # 抽取的视频帧（OCR模式）
    ├── transcript_raw.txt # 语音识别原文
    ├── ocr_raw.txt        # OCR识别原文（如启用）
    └── report.txt         # 格式化的AI报告

示例: output/test_20251211_183934/

🔑 配置文件
───────────────────────────────────────────────────────────────
.env                # API密钥和参数配置

必填：
  GROQ_API_KEY=gsk_xxxxx

可选：
  GROQ_ASR_MODEL=whisper-large-v3-turbo
  GROQ_LLM_MODEL=openai/gpt-oss-120b
  GROQ_MAX_TOKENS=4096
  GROQ_TEMPERATURE=0.7

⚡ 性能对比
───────────────────────────────────────────────────────────────
处理模式对比：
┌──────────┬─────────────┬─────────────┬──────────────┐
│ 模式     │ 处理时间    │ 输出内容    │ 适用场景     │
├──────────┼─────────────┼─────────────┼──────────────┤
│ make run │ ⚡ 快        │ 音频文字    │ 音频为主     │
│ make ocr │ 🐢 慢        │ 音频+画面   │ 画面有文字   │
│下载模式  │ ⚡⚡ 中等    │ 同上+下载   │ 在线视频     │
└──────────┴─────────────┴─────────────┴──────────────┘

OCR模型对比（基于52帧视频测试）：
┌────────────────────┬──────────┬──────────┬──────────┐
│ 模型配置           │ 总耗时   │ 单帧耗时 │ 精确度   │
├────────────────────┼──────────┼──────────┼──────────┤
│ mobile + mobile    │ ~90秒    │ ~1.7秒   │ 85-90%   │
│ mobile + server    │ ~120秒   │ ~2.3秒   │ 90-95%   │
│ server + server    │ ~180秒   │ ~3.5秒   │ 95-98%   │
└────────────────────┴──────────┴──────────┴──────────┘

💡 推荐：mobile模型适合大多数场景，清晰文字识别率已足够

🆘 快速故障排查
───────────────────────────────────────────────────────────────
问题：GROQ_API_KEY 未设置
解决：nano .env （填入真实密钥）

问题：ffmpeg 未安装
解决：brew install ffmpeg

问题：下载失败（yt-dlp 错误）
解决：pip install --upgrade yt-dlp

问题：OCR 处理很慢
解决：使用 mobile 模型（默认）或减少抽帧频率

问题：OCR 识别不准确
解决：使用 server 模型提高精度
      make ocr VIDEO=xxx DET_MODEL=server REC_MODEL=server

问题：虚拟环境损坏
解决：make clean-all && make test （清理并重建）

问题：内存不足
解决：使用 mobile 模型，或减少同时处理的帧数

问题：首次运行很慢
说明：正在自动下载模型和依赖，后续会很快

问题：B站下载失败
解决：安装 BBDown: brew install bbdown

📚 文档
───────────────────────────────────────────────────────────────
docs/
├── AUTO_VENV.md                    # 自动虚拟环境功能说明
├── OCR_MODELS.md                   # OCR 模型选择详细指南
├── DOWNLOAD_GUIDE.md               # 🆕 完整下载功能指南
├── DOWNLOAD_README.md              # 🆕 下载快速参考
├── DOWNLOAD_IMPLEMENTATION.md      # 🆕 下载功能实现细节
├── DRISSIONPAGE_GUIDE.md           # 🆕 DrissionPage 完整使用指南
├── DRISSIONPAGE_SUMMARY.md         # 🆕 DrissionPage 技术总结
├── ARCHIVER_IMAGE_DOWNLOAD.md      # 🆕 图片下载功能文档
├── TWITTER_GUIDE.md                # 🆕 推特平台归档指南
├── GROQ_SETUP.md                   # Groq API 配置指南
├── QUICKSTART.md                   # 快速入门
└── START.md                        # 详细启动指南

README.md                            # 完整项目文档

💡 Pro Tips
───────────────────────────────────────────────────────────────
1. 🆕 首次使用直接开始，无需手动配置：
   make run VIDEO=video.mp4  # 自动完成所有环境配置

2. 🆕 从URL一键下载并处理：
   make download-run URL="https://www.youtube.com/watch?v=xxx"
   make download-ocr URL="https://www.bilibili.com/video/BVxxx"

3. 🆕 支持多个平台的视频，统一处理流程：
   make download-ocr URL="https://example.com/video"
   # 支持 YouTube, Bilibili, 小红书, 抖音, Twitter 等

4. OCR模式带进度条，实时查看处理进度：
   make ocr VIDEO=xxx.mp4  # 显示"🔍 OCR进度: 52/52"

5. 批量处理多个视频：
   for f in *.mp4; do make run VIDEO="$f"; done

6. 使用绝对路径避免问题：
   make run VIDEO="$(pwd)/video.mp4"

7. 查看实时处理日志：
   make run VIDEO=xxx.mp4 2>&1 | tee process.log

8. 自定义输出目录：
   python process_video.py video.mp4 --out-dir /custom/path

9. 根据设备性能选择 OCR 模型：
   # 普通笔记本 → mobile（默认）
   # 高性能工作站 → server
   # 平衡模式 → mobile检测 + server识别

10. OCR流程优化 - 先OCR后音频：
    # 如果OCR识别有问题，可以及早发现
    # 不会浪费时间在音频转写上

11. 清理并重置环境：
    make clean-all  # 删除虚拟环境和输出文件
    make test       # 重新创建环境并测试

12. 查看环境状态：
    make check      # 显示虚拟环境、配置、依赖等状态

🌐 相关链接
───────────────────────────────────────────────────────────────
Groq Console: https://console.groq.com
Groq API Docs: https://console.groq.com/docs
获取API Key: https://console.groq.com/keys

🎓 快速上手步骤
───────────────────────────────────────────────────────────────
1️⃣  确保安装 ffmpeg: brew install ffmpeg
2️⃣  直接运行（首次自动配置）: make run VIDEO=test.mp4
3️⃣  编辑 .env 填入 GROQ_API_KEY（如需AI总结）
4️⃣  开始使用：
    • 本地视频：make run 或 make ocr
    • 网络视频：make download-run 或 make download-ocr

就这么简单！🎉
🌐 网页归档系统 - 快速参考                            ║
║（精准提取正文 + Markdown保存 + 图片下载 + 真实浏览器）║
╚═══════════════════════════════════════════════════════════════╝

🎯 核心功能
───────────────────────────────────────────────────────────────
✅ 智能平台识别（知乎、小红书、B站、Reddit、推特等）
✅ 精准正文提取（自动排除评论、广告、推荐）
✅ Markdown转换（保留图片和格式）
✅ 图片自动下载（HTTP + base64 → 本地JPG）
✅ 两种引擎可选（Crawl4AI 快速 / DrissionPage 真实浏览器）
✅ 自动登录态管理（一次登录长期使用）
✅ 双模式归档（默认纯内容 / 完整含评论）🆕
✅ 批量归档（异步并发处理）
✅ 反爬虫处理（自动UA + Cookie注入 + 真实浏览器指纹）

🚀 快速开始 - 两种引擎选择
───────────────────────────────────────────────────────────────
┌─ 方案1：Crawl4AI（快速归档）─────────────────────────────┐
│ make archive URL=https://www.zhihu.com/question/123       │
│                                                            │
│ 流程：检测平台 → 提取正文 → 转换Markdown → 下载图片        │
│ 输出：archived/平台_标题/                                  │
│       ├── README.md                                       │
│       └── images/                                         │
│ 特点：⚡ 速度快，适合静态页面                              │
│ 适用：简单网页、不需要登录                                 │
└──────────────────────────────────────────────────────────┘

┌─ 方案2：DrissionPage（真实浏览器）🆕 ────────────────────┐
│ # 步骤1：首次登录（仅需一次）                              │
│ make login                                                │
│ # 会打开浏览器窗口，手动完成登录，勾选"记住我"              │
│                                                            │
│ # 步骤2：日常归档（自动使用登录态）                         │
│ make archive URL=链接                            │
│ # 💡 支持分享文本格式（自动提取URL）：                      │
│ make archive URL='【标题】 😆 https://...'       │
│                                                            │
│ # 步骤3：选择归档模式（默认纯内容/完整含评论）🆕            │
│ make archive URL=链接                    # 默认：纯内容   │
│ make archive URL=链接 MODE=full          # 完整：含评论   │
│                                                            │
│ 流程：启动浏览器 → 加载登录态 → 提取 → Markdown → 图片     │
│ 输出：archived/标题/  (不含平台前缀)🆕                     │
│       ├── README.md                                       │
│       └── images/                                         │
│ 特点：🌟 真实浏览器、完整JS、自动登录态、更强反爬           │
│       🌟 自动提取URL（支持从微信/QQ/浏览器复制的分享文本）  │
│       🌟 双模式归档（默认纯内容/完整含评论）                │
│       🌟 文件夹命名更简洁（仅标题，无平台前缀）             │
│ 适用：需要登录、复杂JS、懒加载、反爬严格的平台              │
└──────────────────────────────────────────────────────────┘

🔍 两种引擎对比
───────────────────────────────────────────────────────────────
┌──────────────┬──────────────┬─────────────────┐
│ 特性         │ Crawl4AI     │ DrissionPage    │
├──────────────┼──────────────┼─────────────────┤
│ 速度         │ ⚡⚡⚡⚡⚡  │ ⚡⚡⚡          │
│ JS支持       │ 基础         │ 完整            │
│ 登录态管理   │ 手动配置     │ 自动保存        │
│ 反爬能力     │ 中等         │ 最强            │
│ 资源占用     │ 低           │ 中              │
│ 双模式归档   │ ❌           │ ✅ (default/full)│
│ 适用场景     │ 静态页面     │ 复杂/登录页面   │
│ 命令         │ make archive │ make archive    │
└──────────────┴──────────────┴─────────────────┘

💡 选择建议：
• 简单页面、不需要登录 → Crawl4AI (自动)
• 需要登录、复杂JS渲染 → DrissionPage (自动)
• 反爬严格（小红书、推特）→ DrissionPage (自动)
• 需要纯净内容（无评论）→ MODE=default
• 需要完整内容（含评论）→ MODE=full

┌─ 批量归档 ────────────────────────────────────────────────┐
│ # 创建URL列表文件                                          │
│ cat > urls.txt << EOF                                     │
│ https://www.zhihu.com/question/123                       │
│ https://www.xiaohongshu.com/explore/abc                  │
│ https://www.bilibili.com/read/cv123                      │
│ EOF                                                       │
│                                                            │
│ # 批量归档                                                 │
│ make archive-batch FILE=urls.txt                          │
│                                                            │
│ 特点：并发处理，自动去重，失败重试                          │
└──────────────────────────────────────────────────────────┘

🆕 网页归档 + 数据库集成（推荐）
───────────────────────────────────────────────────────────────
┌─ 归档后自动入库 + AI分析 ────────────────────────────────┐
│ # 基础归档 + AI报告 + 数据库                              │
│ make archive-run URL="https://x.com/user/status/123"    │
│ 流程：归档 → AI分析 → 💾 入库                             │
│ 特点：自动提取标签、生成摘要、全文索引                     │
│                                                            │
│ # 归档 + OCR识别 + AI报告 + 数据库                        │
│ make archive-ocr URL="https://www.zhihu.com/answer/xxx" │
│ 流程：归档 → OCR识别图片 → AI分析 → 💾 入库               │
│ 特点：适合图片较多的网页（知乎、小红书等）                 │
│                                                            │
│ # 调试模式（显示浏览器）                                   │
│ make archive-run-visible URL="链接"                      │
│                                                            │
│ 输出位置：output/语义化文件夹名_时间戳/                   │
│ 数据库：自动保存到 knowledge.db                           │
│ 搜索：立即可用 make search Q="关键词" 搜索                │
│                                                            │
│ 💡 与纯归档的区别：                                        │
│ • make archive        → 仅保存Markdown到 archived/       │
│ • make archive-run    → 归档 + AI报告 + 数据库 + 搜索     │
│ • make archive-ocr    → 归档 + OCR + AI报告 + 数据库      │
└──────────────────────────────────────────────────────────┘

🔐 DrissionPage 登录态管理（推荐）🆕
───────────────────────────────────────────────────────────────
┌─ 一次登录，长期使用 ──────────────────────────────────────┐
│ # 1. 首次使用：浏览器登录（打开窗口）                       │
│ make login                    # 通用登录（支持多平台）      │
│ make login-twitter            # 推特专用登录 🆕             │
│ → 会打开浏览器窗口                                         │
│ → 手动访问平台（知乎/小红书/B站/推特）                     │
│ → 完成登录，勾选"记住我"                                   │
│ → 关闭窗口，登录数据自动保存                               │
│                                                            │
│ # 2. 验证登录状态                                          │
│ make test-twitter-cookie      # 测试推特 Cookie 🆕         │
│                                                            │
│ # 3. 日常归档：自动使用登录态                              │
│ make archive URL=https://www.xiaohongshu.com/...          │
│ make archive URL=https://twitter.com/user/status/123 🆕   │
│ → 无头模式后台运行                                         │
│ → 自动加载登录态                                           │
│ → 无需任何额外参数                                         │
│                                                            │
│ # 4. 选择归档模式 🆕                                       │
│ make archive URL=链接                     # 默认：纯内容  │
│ make archive URL=链接 MODE=full           # 完整：含评论  │
│                                                            │
│ # 5. 登录态过期：重新登录                                  │
│ make reset-browser  # 清空旧数据                          │
│ make login          # 重新登录                             │
│                                                            │
│ 🎉 优势：                                                  │
│ • 一次登录，所有平台都能用（知乎+小红书+B站+推特+...）     │
│ • 自动保存 Cookies + LocalStorage + 浏览器指纹             │
│ • 无需手动复制Cookie                                       │
│ • 真实浏览器环境，不易被检测                               │
│ • 支持扫码登录、短信验证等复杂流程                         │
│                                                            │
│ 存储位置：browser_data/                                    │
└──────────────────────────────────────────────────────────┘

🍪 Crawl4AI Cookie 配置（传统方式）
───────────────────────────────────────────────────────────────
┌─ 小红书Cookie配置（一次配置，两处使用）───────────────────┐
│ # 1. 配置Cookie（与视频下载共享）                          │
│ make config-xhs-cookie                                    │
│ # 按提示粘贴Cookie                                         │
│                                                            │
│ # 2. 自动使用（无需额外参数）✨                            │
│ make archive URL=https://www.xiaohongshu.com/explore/xxx │
│ make download URL=https://www.xiaohongshu.com/explore/xxx│
│                                                            │
│ 🎉 特点：                                                  │
│ • 配置一次，视频下载和网页归档都能用                        │
│ • 自动检测小红书URL并加载Cookie                            │
│ • 适合脚本自动化                                           │
│                                                            │
│ 存储位置：XHS-Downloader/Volume/settings.json             │
└──────────────────────────────────────────────────────────┘

┌─ 其他方式（可选）────────────────────────────────────────┐
│ # 使用浏览器Cookie                                         │
│ python -m cli.archive_cli URL --browser chrome           │
│                                                            │
│ # 使用Cookie文件                                           │
│ python -m cli.archive_cli URL --cookies cookies.txt      │
│                                                            │
│ 支持浏览器：chrome, firefox, edge, safari                 │
└──────────────────────────────────────────────────────────┘

🎨 支持的平台
───────────────────────────────────────────────────────────────
┌──────────┬─────────────────────┬─────────┬──────────┐
│ 平台     │ URL示例             │ 需登录  │ 提取内容 │
├──────────┼─────────────────────┼─────────┼──────────┤
│ 知乎     │ zhihu.com/question/ │ ❌      │ 回答正文 │
│ 小红书   │ xiaohongshu.com/... │ ✅      │ 笔记+图片│
│ B站      │ bilibili.com/read/  │ ❌      │ 专栏文章 │
│ 推特/X🆕│ twitter.com/status/ │ ✅      │ 推文+图片│
│ Reddit   │ reddit.com/r/.../   │ ❌      │ 帖子内容 │
│ WordPress│ 通用博客网站         │ ❌      │ 文章主体 │
└──────────┴─────────────────────┴─────────┴──────────┘

💡 自动特性：
• 检测URL自动选择对应平台适配器
• 自动排除评论、广告、侧边栏
• 保留图片链接（Markdown格式）
• 生成元数据（标题、URL、归档时间）
• 🆕 支持双模式（默认纯内容/完整含评论）

📝 命令速查
───────────────────────────────────────────────────────────────
🆕 DrissionPage（真实浏览器）- 统一归档命令：
  make login                            # 通用登录（首次）
  make login-twitter                    # 推特专用登录 🆕
  make test-twitter-cookie              # 测试推特 Cookie 🆕
  make archive URL=网址                  # 归档（默认模式：纯内容）
  make archive URL=网址 MODE=full        # 归档（完整模式：含评论）🆕
  make export-twitter-cookies           # 导出推特 Cookie 🆕
  make reset-browser                    # 重置浏览器数据

Crawl4AI（快速归档）：
  make archive URL=网址                  # 归档单个网页
  make archive-batch FILE=urls.txt      # 批量归档
  make archive-detect URL=网址           # 检测平台
  make test-archiver                    # 测试功能

配置Cookie（Crawl4AI）：
  make config-xhs-cookie                # 配置小红书Cookie
  make config-zhihu-cookie              # 配置知乎Cookie

清理归档：
  make clean-archived                   # 清理归档输出

Python命令行：
  python -m cli.archive_cli URL                    # 基本使用
  python -m cli.archive_cli URL -o output_dir     # 指定输出

🎯 平台归档示例
───────────────────────────────────────────────────────────────
  🆕 DrissionPage 统一归档（推荐）
make login                    # 首次：打开浏览器手动登录
make archive URL=https://www.xiaohongshu.com/explore/abc123
make archive URL=https://www.zhihu.com/question/12345/answer/67890
make archive URL=https://twitter.com/user/status/123456 🆕

# 🆕 推特归档（专用流程）
make login-twitter            # 推特专用登录
make test-twitter-cookie      # 验证登录状态
make archive URL=https://twitter.com/elonmusk/status/123
make archive URL=https://x.com/user/status/456  # x.com 自动转换

# 🆕 双模式归档（默认 vs 完整）
make archive URL=链接                  # 默认：纯内容（无评论）
make archive URL=链接 MODE=full        # 完整：含评论区

# Crawl4AI 方式（简单静态页面）
make archive URL=https://www.zhihu.com/question/12345/answer/67890
make archive URL=https://www.reddit.com/r/python/comments/abc123/

# 小红书笔记（两种方式）
# 方式1: DrissionPage（推荐）
make login
make drission-archive URL=https://www.xiaohongshu.com/explore/abc123

# 方式2: Crawl4AI（需配置Cookie）
make config-xhs-cookie
make archive URL=https://www.xiaohongshu.com/explore/abc123

# B站专栏
make archive URL="https://www.bilibili.com/read/cv12345678"

# 批量归档
cat > xhs_urls.txt << EOF
https://www.xiaohongshu.com/explore/abc123
https://www.xiaohongshu.com/explore/def456
EOF
make archive-batch FILE=xhs_urls.txt

📂 输出格式
───────────────────────────────────────────────────────────────
输出目录：archived/（自动创建）
文件夹结构：标题/  (不含平台前缀)🆕

示例：
archived/
└── 为什么INTP有很强的小猫特质？/    # 🆕 简洁命名
    ├── README.md              # Markdown 内容
    └── images/                # 下载的图片
        ├── image_abc123.jpg
        └── image_def456.jpg

README.md 内容：
---
title: 文章标题
url: https://example.com
platform: xiaohongshu
archived_at: 2026-01-07 12:34:56
---

正文内容...
图片引用: ![](images/image_abc123.jpg)

🆕 默认模式 vs 完整模式：
  默认模式 (MODE=default)：
    • 纯净内容，无评论、无用户profile
    • 文件夹命名：标题（无平台前缀）
    • 适合：知识收集、内容存档
  
  完整模式 (MODE=full)：
    • 包含所有内容（评论、互动、推荐）
    • 适合：完整记录、数据分析

⚡ 高级选项
───────────────────────────────────────────────────────────────
并发控制：
  python -m cli.archive_cli -f urls.txt -c 5    # 5个并发

自定义输出：
  make archive URL=xxx OUTPUT=my_archives       # 指定目录

调试模式：
  python -m cli.archive_cli URL --show-browser -v
  # 显示浏览器窗口 + 详细日志

平台检测：
  make archive-detect URL=https://example.com
  # 仅检测平台，不归档

🔧 Cookie配置详解
───────────────────────────────────────────────────────────────
小红书Cookie获取方法：
1. 浏览器访问：https://www.xiaohongshu.com/explore
2. 登录账号
3. F12打开开发者工具
4. Network标签 → 刷新页面
5. 找到任意请求 → Request Headers
6. 复制完整的Cookie值
7. 运行 make config-xhs-cookie 粘贴

Cookie优先级：
1. XHS-Downloader配置（推荐，自动）
2. 浏览器Cookie（--browser参数）
3. Cookie文件（--cookies参数）

Cookie验证：
  python scripts/test_cookie_unified.py
  # 检查Cookie是否配置成功

🛠️ 故障排查
───────────────────────────────────────────────────────────────
问题：提示 Crawl4AI 未安装
解决：
  pip install crawl4ai
  playwright install chromium

问题：小红书归档失败
解决：
  1. 配置Cookie：make config-xhs-cookie
  2. 验证配置：python scripts/test_cookie_unified.py
  3. 或手动指定：python -m cli.archive_cli URL --browser chrome

问题：内容提取不完整
解决：
  # 使用调试模式查看
  python -m cli.archive_cli URL --show-browser -v

问题：403错误
解决：
  1. 配置Cookie（如小红书）
  2. 检查User-Agent
  3. 降低并发数：-c 1

问题：批量归档部分失败
说明：
  # 正常现象，会显示失败的URL
  # 可以单独重试失败的URL

📊 性能建议
───────────────────────────────────────────────────────────────
• 单个归档：约3-10秒（取决于网页复杂度）
• 批量归档：建议并发数3-5
• Cookie有效期：定期更新（通常几周到几个月）
• 归档频率：避免过于频繁请求同一网站

💡 Pro Tips - 归档篇
───────────────────────────────────────────────────────────────
1. 🍪 一次配置，两处使用：
   make config-xhs-cookie  # 视频下载和网页归档都能用

2. 🚀 自动化归档：
   # 小红书会自动使用配置的Cookie，无需每次指定
   make archive URL=https://www.xiaohongshu.com/explore/xxx

3. 📦 批量处理：
   # 创建URL列表，一次性归档多个网页
   make archive-batch FILE=urls.txt

4. 🔍 验证配置：
   # 测试Cookie和归档功能
   python scripts/test_cookie_unified.py
   python scripts/verify_archiver.py

5. 🐛 调试问题：
   # 显示浏览器窗口，查看实际加载内容
   python -m cli.archive_cli URL --show-browser -v

6. 🎯 平台检测：
   # 不确定URL属于哪个平台时
   make archive-detect URL=https://example.com

7. 📁 组织归档：
   # 使用不同输出目录管理不同主题的归档
   make archive URL=xxx OUTPUT=research/papers

8. ⚡ 性能优化：
   # 批量时控制并发数
   python -m cli.archive_cli -f urls.txt -c 3

9. 🔄 定期更新Cookie：
   # Cookie过期时重新配置
   make config-xhs-cookie

10. 📚 查看文档：
    cat docs/ARCHIVER_GUIDE.md        # 完整指南
    cat docs/ARCHIVER_QUICKREF.md     # 快速参考
    cat docs/COOKIE_UNIFIED.md        # Cookie管理

📚 归档文档
───────────────────────────────────────────────────────────────
docs/
├── ARCHIVER_GUIDE.md              # 完整使用指南
├── ARCHIVER_QUICKREF.md           # 快速参考
├── ARCHIVER_IMPLEMENTATION.md     # 实现说明
├── COOKIE_UNIFIED.md              # Cookie统一管理
└── COOKIE_INTEGRATION.md          # Cookie整合报告

archiver/
└── README.md                      # 模块说明

🎓 归档快速上手
───────────────────────────────────────────────────────────────
1️⃣  安装依赖：
    make install        # 安装所有依赖

2️⃣  配置Cookie（如需要）：
    make config-xhs-cookie     # 小红书必需

3️⃣  开始归档：
    # 知乎（无需Cookie）
    make archive URL=https://www.zhihu.com/question/123
    
    # 小红书（自动使用配置）
    make archive URL=https://www.xiaohongshu.com/explore/abc

4️⃣  查看结果：
    ls archived/
    cat archived/zhihu_*.md

就这么简单！🎉

🌟 归档功能亮点
───────────────────────────────────────────────────────────────
✨ 智能识别：自动检测平台，选择最佳提取策略
✨ 精准提取：只保存正文，排除评论、广告、推荐
✨ 统一管理：Cookie配置复用，视频下载+网页归档
✨ 批量处理：异步并发，高效归档多个URL
✨ 格式标准：Markdown输出，保留图片和格式
✨ 反爬处理：自动UA、Cookie注入、降级策略
✨ 易于扩展：模块化设计，轻松添加新平台

🔗 归档 + 视频下载联动
───────────────────────────────────────────────────────────────
完整工作流示例：

1️⃣  配置小红书Cookie（一次性）：
    make config-xhs-cookie

2️⃣  归档小红书笔记（获取内容）：
    make archive URL=https://www.xiaohongshu.com/explore/abc123
    # 输出：archived/xiaohongshu_1234_20260107.md

3️⃣  下载小红书视频（如果有视频）：
    make download URL=https://www.xiaohongshu.com/explore/abc123
    # 输出：videos/xiaohongshu_abc123_标题.mp4

4️⃣  处理视频（转写+OCR+总结）：
    make ocr VIDEO=videos/xiaohongshu_abc123_标题.mp4
    # 自动入库，可搜索

🎯 一次Cookie配置，完整内容获取！
• 归档笔记文本
• 下载配套视频
• 处理视频入库
• 全文搜索可查

═══════════════════════════════════════════════════════════════

╔═══════════════════════════════════════════════════════════════╗
║          
═══════════════════════════════════════════════════════════════

╔═══════════════════════════════════════════════════════════════╗
║          🗄️  数据库与搜索系统 - 快速参考                      ║
║         （视频内容持久化 + 智能检索）                          ║
╚═══════════════════════════════════════════════════════════════╝

🎯 核心功能
───────────────────────────────────────────────────────────────
✅ 自动保存所有处理结果到数据库
✅ 全文搜索（支持中英文混合）
✅ 按标签快速筛选
✅ 时间戳精确定位（可跳转到视频）
✅ 视频去重（自动跳过已处理）
✅ 智能标签管理

🚀 快速部署（3步）
───────────────────────────────────────────────────────────────
1️⃣  初始化数据库：
    make db-init

2️⃣  测试功能：
    make db-test

3️⃣  开始使用：
    # 处理视频会自动入库
    make run VIDEO=video.mp4
    
    # 搜索内容
    make search Q="关键词"

🔍 搜索命令
───────────────────────────────────────────────────────────────
┌─ 全文搜索（默认模糊搜索） ──────────────────────────────────┐
│ make search Q="关键词"                                      │
│ make search Q="机器学习"                                    │
│ make search Q="deep learning"                              │
│ make search Q="神经 网络"                                   │
│                                                            │
│ 默认行为：模糊匹配（智能查找相关内容）                      │
│ 精确搜索：FLAGS="--exact"                                  │
│ 搜索范围：报告、转写、OCR、主题（全部内容）                │
│ 返回结果：匹配片段 + 时间戳 + 标签 + 相关性分数            │
└──────────────────────────────────────────────────────────┘

┌─ 高级搜索选项 ────────────────────────────────────────────┐
│ 多关键词（默认OR）：                                       │
│   make search Q="机器学习 深度学习"                         │
│   # 匹配包含"机器学习"或"深度学习"的内容                    │
│                                                            │
│ 多关键词AND：                                              │
│   make search Q="深度学习 神经网络" FLAGS="--and"           │
│   # 必须同时包含两个词                                     │
│                                                            │
│ 精确搜索：                                                 │
│   make search Q="python编程" FLAGS="--exact"               │
│   # 完全匹配，不做模糊处理                                 │
│                                                            │
│ 指定字段：                                                 │
│   make search Q="代码" FLAGS="--field ocr"                 │
│   # 仅搜索OCR识别的文字                                    │
│                                                            │
│ 排序方式：                                                 │
│   make search Q="机器学习" FLAGS="--sort date"             │
│   # 按日期排序（默认按相关性）                             │
└──────────────────────────────────────────────────────────┘

┌─ 按标签搜索 ──────────────────────────────────────────────┐
│ make search-tags TAGS="标签1 标签2"                        │
│ make search-tags TAGS="教育 科技"                          │
│ make search-tags TAGS="机器学习 深度学习"                  │
│                                                            │
│ 逻辑：AND（必须包含所有标签）                               │
│ 返回：符合条件的所有视频                                   │
└──────────────────────────────────────────────────────────┘

┌─ 搜索主题 ────────────────────────────────────────────────┐
│ make search-topics Q="主题关键词"                           │
│ make search-topics Q="卷积神经网络"                         │
│                                                            │
│ 搜索范围：主题标题 + 主题摘要                               │
│ 返回：匹配的章节 + 时间范围                                 │
└──────────────────────────────────────────────────────────┘

┌─ Whoosh 索引管理（高级） ───────────────────────────────────┐
│ make whoosh-rebuild          # 重建 Whoosh 全文索引        │
│ make whoosh-search Q="中文"  # 直接用 Whoosh 搜索          │
│                                                            │
│ 💡 中文搜索自动使用 Whoosh + jieba 分词，效果更好          │
│ • 首次运行 make db-init 会自动构建 Whoosh 索引             │
│ • 数据变化后建议 make whoosh-rebuild 重建索引              │
│ • 存储位置：storage/whoosh_index/                          │
└──────────────────────────────────────────────────────────┘

🏷️  标签管理
───────────────────────────────────────────────────────────────
查看热门标签：
  make db-tags

列出所有视频（名称+标签+摘要）：
  make db-list              # 默认列出前 20 条
  make db-list LIMIT=50     # 列出前 50 条
  make ls                   # 快捷命令

查看特定视频详情：
  make db-show ID=1         # 查看视频基本信息
  make db-show ID=1 FLAGS="--full"   # 查看完整内容（转写+OCR）
  make db-show ID=1 FLAGS="--json"   # JSON格式输出

标签自动补全（命令行工具）：
  python search_cli.py suggest "机器"

💡 标签自动提取
  • 处理视频时 LLM 自动提取标签
  • 可手动添加/修改标签（Python API）

🗄️  数据库管理
───────────────────────────────────────────────────────────────
💡 自动入库
  • 🎬 运行 make run、make ocr、make download-run 等命令后
  • 💾 自动将处理结果保存到数据库
  • 🏷️  AI自动提取标签和主题章节
  • 🔍 全文搜索索引自动更新
  • ⚡ 立即可以使用搜索命令查询

初始化数据库：
  make db-init

查看数据库状态：
  make db-status                # 📊 快速概览（5大类统计）
                                # ├─ 基础统计（视频数、标签数、FTS索引等）
                                # ├─ 按来源统计（本地/B站/Twitter/知乎等）
                                # ├─ 处理状态（已完成/待处理/失败）
                                # ├─ 内容类型（视频文件/网页归档/OCR/报告）
                                # └─ 健康状况（7天活跃度/成功率）

查看详细统计：🆕
  make db-stats                 # 所有详细统计（归档+视频+标签）
  make db-stats-archives        # 🌐 网页归档专项统计
                                # ├─ 按平台统计（Twitter/知乎/Reddit等）
                                # ├─ OCR使用率
                                # └─ 最近归档列表
  make db-stats-videos          # 🎥 视频文件专项统计
                                # ├─ 按平台统计（含时长、文件大小）
                                # ├─ 视频总时长
                                # └─ OCR使用率
  make db-stats-tags            # 🏷️  标签使用统计
                                # ├─ Top 20 最常用标签
                                # └─ 标签使用分布

查看特定记录：
  make db-show ID=1             # 查看ID=1的详细信息
  make db-show ID=1 FLAGS="--full"  # 查看完整内容（含转写/OCR）

删除记录：🆕
  make db-delete ID=1           # 删除记录（需确认）
  make db-delete ID=1 FORCE=1   # 强制删除（无需确认）
                                # ⚠️  只删除数据库记录，不删除文件

测试所有功能：
  make db-test                  # 运行单元测试
  make db-import-test          # 导入 output 真实数据测试

重建数据库（删除所有数据）：
  make db-reset

备份数据库：
  make db-backup

优化数据库（定期维护）：
  make db-vacuum

📊 搜索返回结果示例
───────────────────────────────────────────────────────────────
╭─────────────────────────────────────────────────────────────╮
│ 🔍 找到 3 个结果:                                            │
│                                                             │
│ [1] 机器学习基础教程                                         │
│     来源: transcript                                         │
│     片段: ...介绍神经网络的基本概念和结构...                  │
│     时间: 02:15                                             │
│     相关性: 0.85                                            │
│     标签: 教育, 机器学习, AI                                 │
│                                                             │
│ [2] 深度学习实战                                             │
│     来源: report                                            │
│     片段: ...卷积神经网络（CNN）的应用...                     │
│     时间: 05:32                                             │
│     相关性: 0.78                                            │
│     标签: 深度学习, CNN, 计算机视觉                          │
╰─────────────────────────────────────────────────────────────╯

🎯 高级用法（Python API）
───────────────────────────────────────────────────────────────
from db import SearchRepository

repo = SearchRepository()

# 全文搜索
results = repo.search(
    query="机器学习",
    tags=["教育", "科技"],
    fields='transcript',      # report/transcript/ocr/all
    sort_by='relevance',      # relevance/date/duration
    limit=20
)

# 处理结果
for result in results:
    print(f"视频: {result.video_title}")
    print(f"时间: {result.timestamp_seconds}s")
    print(f"片段: {result.matched_snippet}")
    print(f"文件: {result.file_path}")

📂 数据库文件位置
───────────────────────────────────────────────────────────────
storage/
├── database/
│   └── knowledge.db          # 主数据库（SQLite）
├── backups/                  # 备份目录
│   └── knowledge_backup_*.db
├── videos/                   # 原视频文件
└── artifacts/                # 处理产物
    └── {video_hash}/
        ├── frames/           # 抽帧图片
        ├── transcript.json
        ├── ocr.json
        └── report.md

📊 数据库表结构
───────────────────────────────────────────────────────────────
videos           → 视频元信息（来源、时长、状态）
artifacts        → 转写/OCR/报告产物
tags             → 标签体系
video_tags       → 视频-标签关联
topics           → 主题/章节
timeline_entries → 时间线（秒级精度）
fts_content      → 全文搜索索引（FTS5）
embeddings       → 向量表（预留，未来扩展）
processing_logs  → 处理日志

💡 Pro Tips - 数据库篇
───────────────────────────────────────────────────────────────
1. 自动入库：
   # 处理视频时自动保存到数据库，无需额外操作
   make run VIDEO=video.mp4

2. 搜索带时间戳：
   # 搜索结果包含精确时间点，可直接跳转播放
   make search Q="关键词"

3. 视频自动去重：
   # 基于文件内容 hash，重复视频自动跳过
   make run VIDEO=same_video.mp4  # 第二次会提示已存在

4. 批量搜索：
   # 使用命令行工具进行复杂查询
   python search_cli.py search "关键词" --field transcript --json

5. 定期备份：
   # 建议每周备份一次
   make db-backup

6. 性能优化：
   # 每处理1000个视频后执行一次
   make db-vacuum

7. 查看统计信息：
   make db-status
   # 显示：视频数、产物数、标签数、数据库大小

8. 标签过滤组合：
   # 查找同时包含多个标签的视频
   make search-tags TAGS="机器学习 深度学习 Python"

9. 搜索特定字段：
   make search Q="代码" FLAGS="--field ocr"  # 仅搜索OCR
   make search Q="演讲" FLAGS="--field transcript"  # 仅搜索转写

10. 导出搜索结果：
    python search_cli.py search "关键词" --json > results.json

11. 时间范围搜索（Python API）：
    # 在代码中可以按处理时间过滤
    from db import VideoRepository
    videos = repo.list_videos(limit=100)

12. SQLite 直接查询（高级）：
    sqlite3 storage/database/knowledge.db
    SELECT title, duration_seconds FROM videos;

🔧 故障排查 - 数据库
───────────────────────────────────────────────────────────────
问题：数据库文件不存在
解决：make db-init

问题：搜索无结果
解决：
  1. 确认已处理视频并入库
  2. 检查搜索关键词是否正确
  3. 尝试简化搜索词

问题：数据库锁定
解决：
  sqlite3 storage/database/knowledge.db "PRAGMA wal_checkpoint;"

问题：搜索很慢
解决：
  1. make db-vacuum  # 优化数据库
  2. 减少返回结果数量（--limit 参数）

问题：想清空数据库重新开始
解决：make db-reset

📚 数据库文档
───────────────────────────────────────────────────────────────
docs/
├── DATABASE_DESIGN.md        # 完整设计文档
├── DATABASE_QUICKSTART.md    # 快速开始指南
├── DATABASE_SUMMARY.md       # 实施总结
└── DATABASE_README.md        # 功能概览

🎓 数据库快速上手
───────────────────────────────────────────────────────────────
1️⃣  首次使用：
    make db-init         # 初始化数据库
    make db-test         # 测试功能

2️⃣  处理视频：
    make run VIDEO=test.mp4    # 自动入库

3️⃣  搜索内容：
    make search Q="测试"       # 全文搜索
    make db-tags               # 查看标签
    make db-status             # 查看统计

4️⃣  定期维护：
    make db-backup      # 每周备份
    make db-vacuum      # 每月优化

就这么简单！🎉

🌟 数据库功能亮点
───────────────────────────────────────────────────────────────
✨ 零配置：SQLite 单文件，无需安装额外服务
✨ 全文搜索：FTS5 引擎，支持中英文混合
✨ 智能去重：SHA256 hash 自动识别重复视频
✨ 时间定位：搜索结果精确到秒，可跳转播放
✨ 标签系统：自动提取主题关键词

╔═══════════════════════════════════════════════════════════════╗
║         🐦 推特(Twitter/X) 归档 - 完整示例 🆕                 ║
╚═══════════════════════════════════════════════════════════════╝

🎯 快速开始（三步走）
───────────────────────────────────────────────────────────────
1️⃣ 首次登录（只需一次）
   make login-twitter
   # 在打开的浏览器中登录推特账号

2️⃣ 验证登录
   make test-twitter-cookie
   # 看到 ✅ 表示登录成功

3️⃣ 开始归档
   make archive URL="https://twitter.com/elonmusk/status/123"
   # 或
   make archive URL="https://x.com/user/status/456"

📝 归档模式对比
───────────────────────────────────────────────────────────────
┌────────────────┬─────────────────┬─────────────────┐
│ 模式           │ 默认模式        │ 完整模式        │
├────────────────┼─────────────────┼─────────────────┤
│ 命令           │ MODE=default    │ MODE=full       │
│ 推文正文       │ ✅              │ ✅              │
│ 图片和视频     │ ✅              │ ✅              │
│ 用户profile    │ ❌ 自动移除     │ ✅              │
│ 互动数字       │ ❌ 自动移除     │ ✅              │
│ 评论区         │ ❌ 自动排除     │ ✅              │
│ 侧边栏推荐     │ ❌ 自动排除     │ ❌              │
│ 适用场景       │ 知识收集        │ 完整记录        │
└────────────────┴─────────────────┴─────────────────┘

💡 使用示例：
  # 默认模式：纯内容（推荐日常使用）
  make archive URL="https://twitter.com/user/status/123"
  
  # 完整模式：含评论和互动
  make archive URL="https://twitter.com/user/status/123" MODE=full

🔐 登录管理
───────────────────────────────────────────────────────────────
登录推特：
  make login-twitter

测试 Cookie：
  make test-twitter-cookie
  # 输出示例：
  # ✅ auth_token: abc123...
  # ✅ ct0: xyz789...
  # ✅ 已登录！

导出 Cookie：
  make export-twitter-cookies
  # 导出到 cookies/twitter.txt

重置登录：
  make reset-browser
  make login-twitter

📂 输出示例
───────────────────────────────────────────────────────────────
输出位置：archived/推文标题/

示例结构：
archived/
└── The future of AI is exciting/
    ├── README.md
    └── images/
        └── tweet_image_001.jpg

README.md 内容：
---
title: The future of AI is exciting
url: https://twitter.com/user/status/123
platform: twitter
archived_at: 2026-01-07 12:34:56
---

推文正文内容...

![](images/tweet_image_001.jpg)

🛠️ 故障排除
───────────────────────────────────────────────────────────────
❌ 问题：403/401 错误
✅ 解决：
   make test-twitter-cookie    # 检查登录状态
   make reset-browser          # 清空数据
   make login-twitter          # 重新登录

❌ 问题：Cookie 过期
✅ 解决：
   推特 Cookie 通常有效期很长（数周），过期后重新登录即可

❌ 问题：x.com 链接
✅ 解决：
   自动转换为 twitter.com，无需手动修改

📚 更多信息
───────────────────────────────────────────────────────────────
完整文档：docs/TWITTER_GUIDE.md
技术细节：archiver/platforms/twitter.py

6-01-07
✨ 版本：v3.1 - 网页归档 + 推特支持 + 数据库 + 统一下载

═══════════════════════════════════════════════════════════════
📌 最后更新：2026-01-07
✨ 版本：v3.1 - 推特归档 + 双模式 + 网页归档 + 数据库
打印或保存这张卡片，随时查阅！🎯

# 🚀 快速启动指南

## 一、首次使用（3 步设置）

### 1️⃣ 初始化环境
```bash
cd /Users/catherina/Documents/GitHub/knowledge/video_report
make setup
```
这会自动：
- ✅ 创建 Python 虚拟环境
- ✅ 安装所有依赖
- ✅ 创建 .env 配置文件
- ✅ 运行环境测试

### 2️⃣ 配置 API Key
```bash
nano .env
```
填入你的 Groq API Key：
```env
GROQ_API_KEY=gsk_你的真实密钥
```

保存并退出（Ctrl+O, Enter, Ctrl+X）

### 3️⃣ 验证配置
```bash
make check
```

---

## 二、日常使用（2 种模式）

### 🎵 模式 1：仅音频转文字 + AI 总结（快速）

**适用场景：**
- 播客、讲座、会议录音
- 视频画面不重要
- 需要快速处理

**命令：**
```bash
make run VIDEO=/path/to/video.mp4
```

**示例：**
```bash
make run VIDEO=~/Downloads/meeting.mp4
make run VIDEO=~/Desktop/lecture.mp4
```

**流程：**
```
视频 → 音频提取 → Whisper转写 → GPT-OSS总结 → 报告
⏱️  速度：快（几分钟）
```

---

### 🔍 模式 2：音频 + OCR + AI 总结（完整）

**适用场景：**
- 教学视频（有 PPT/代码）
- 演示视频（有屏幕文字）
- 需要提取画面文字

**命令：**
```bash
make ocr VIDEO=/path/to/video.mp4
```

**示例：**
```bash
make ocr VIDEO=~/Downloads/tutorial.mp4
make ocr VIDEO=~/Desktop/presentation.mp4
```

**流程：**
```
视频 → 音频提取 + 视频抽帧 → Whisper转写 + OCR识别 → 合并 → GPT-OSS总结 → 报告
⏱️  速度：慢（十几分钟，取决于视频长度）
```

---

## 三、查看结果

### 查看最新报告
```bash
make show-report
```

### 列出所有报告
```bash
make list-reports
```

### 手动查看
```bash
# 报告文件
cat output/reports/视频名_report.txt

# 音频文件
ls output/audio/

# OCR 抽帧图片（如果用了 ocr 模式）
ls output/frames/
```

---

## 四、其他命令

### 查看所有可用命令
```bash
make help
```

### 运行环境测试
```bash
make test
```

### 清理输出文件
```bash
make clean
```

### 重新安装依赖
```bash
make install
```

---

## 五、完整工作流示例

### 示例 1：快速处理会议录音
```bash
# 1. 处理视频（仅音频）
make run VIDEO=~/Downloads/meeting.mp4

# 2. 查看报告
make show-report

# 3. 或直接打开文件
open output/reports/meeting_report.txt
```

### 示例 2：处理教学视频（含 PPT）
```bash
# 1. 完整处理（音频 + OCR）
make ocr VIDEO=~/Downloads/lecture.mp4

# 2. 查看报告
cat output/reports/lecture_report.txt

# 3. 查看抽取的帧
open output/frames/lecture/
```

---

## 六、常见问题

### ❌ 错误：GROQ_API_KEY 未设置
```bash
# 解决方法：
nano .env
# 填入真实的 API Key
```

### ❌ 错误：ffmpeg 未安装
```bash
# 解决方法：
brew install ffmpeg
```

### ❌ 错误：虚拟环境不存在
```bash
# 解决方法：
make setup
```

### ⚠️  处理很慢
- **音频模式**通常很快（几分钟）
- **OCR 模式**会慢很多（十几分钟）
- OCR 第一次运行会下载模型（~100MB）

---

## 七、对比两种模式

| 特性 | `make run` | `make ocr` |
|------|-----------|-----------|
| **处理内容** | 仅音频 | 音频 + 画面文字 |
| **速度** | ⚡ 快 | 🐢 慢 |
| **适用场景** | 播客、会议 | 教学、演示 |
| **输出** | 音频转写 + 总结 | 音频 + OCR + 总结 |
| **成本** | 低 | 较高（多了 OCR） |

---

## 八、Pro Tips 💡

1. **批量处理**：写个循环
   ```bash
   for video in ~/Downloads/*.mp4; do
       make run VIDEO="$video"
   done
   ```

2. **调整 OCR 语言**
   ```bash
   # 编辑 process_video.py
   # 或在命令行传参：--ocr-lang en
   ```

3. **查看 API 使用情况**
   访问：https://console.groq.com/settings/limits

4. **定制总结 prompt**
   编辑 `process_video.py` 中的 `summarize_with_gpt_oss_120b()` 函数

---

## 九、项目结构

```
video_report/
├── Makefile              ← 快速命令入口
├── process_video.py      ← 主程序
├── ocr_utils.py          ← OCR 工具
├── .env                  ← 你的 API 配置
├── output/               ← 所有输出文件
│   ├── audio/           ← 音频文件
│   ├── frames/          ← 抽帧图片
│   └── reports/         ← 生成的报告
└── .venv/               ← Python 虚拟环境
```

---

## 🎯 记住这两个核心命令

```bash
# 快速模式（仅音频）
make run VIDEO=视频路径

# 完整模式（音频+OCR）
make ocr VIDEO=视频路径
```

就这么简单！🚀
